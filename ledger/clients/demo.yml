# The platform's OWN public demo (demo.octbase.io) — not a paying client, but
# ledger-managed like one since its migration off the shared claude account
# (playbooks/migrate-instance.yml; see the migrate-instance skill for the
# runbook). Special: its .env sets OCTBASE_DEMO_MODE=true (seeded demo
# logins), which the client compose override passes through.
name: demo
display_name: Public demo (demo.octbase.io)
contact: lars@beyags.com
edition: enterprise
jira_import: true
max_users: 5
registered: '2026-07-11'
status: active
app_version: '1.0.2'
host: prod01
disk_quota_gb: 10
ports:
  frontend: 8110
  api: 8111
  postgres: 8112
notes: >-
  Platform demo instance. Migrated from its legacy shared-account checkout
  under /home/claude (since removed) into the oct-demo account on 2026-07-11
  via migrate-instance.yml (legacy ports 8080/8000/5432 stay reserved).
