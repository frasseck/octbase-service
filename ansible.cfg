[defaults]
inventory = inventory/hosts.yml
interpreter_python = auto_silent
retry_files_enabled = False
# Show diffs for template/lineinfile changes — the playbooks are the audit trail.
[diff]
always = True

[ssh_connection]
pipelining = True
